# E键库存界面 - 完全重构说明

## 概述

E键库存界面已完全重构，用自定义背包系统替代原生Minecraft库存。

## 布局设计

```
┌─────────────────────────────────────────────────────────────┐
│ E KEY INVENTORY SCREEN (1920x1080)                          │
├──────────────────┬──────────────────────────┬────────────────┤
│   STATUS PANEL   │  SCROLLABLE INVENTORY    │   RESERVED     │
│   (Left 150px)   │   (Center 280px)         │   (Right)      │
│                  │                          │                │
│ ┌──────────────┐ │ ┌──────────────────────┐ │                │
│ │ Status       │ │ │ Inventory            │ │                │
│ │              │ │ │                      │ │                │
│ │ HEAD: 100 HP │ │ │ ┌────────────────┐   │ │                │
│ │ ████████████ │ │ │ │ pockets (2x5)  │   │ │                │
│ │              │ │ │ │ ┌─────────────┐│   │ │                │
│ │ CHEST: 100   │ │ │ │ │             ││   │ │                │
│ │ ████████████ │ │ │ │ └─────────────┘│   │ │                │
│ │              │ │ │ └────────────────┘   │ │                │
│ │ (更多部位)   │ │ │                      │ │                │
│ │              │ │ │ ┌────────────────┐   │ │                │
│ │              │ │ │ │ tactical_vest  │   │ │ [可滚动]      │
│ │              │ │ │ │ (8x8)          │   │ │ ↑↓           │
│ └──────────────┘ │ │ │ ...            │   │ │                │
│                  │ │ └────────────────┘   │ │                │
└──────────────────┴──────────────────────────┴────────────────┘
```

## 功能说明

### 左侧状态面板 (Status Panel)
- **宽度**: 150像素
- **显示内容**:
  - 玩家所有身体部位血量
  - 每个部位有血量数值显示
  - 彩色血量条（绿→红渐变）
  - 标题: "Status"

### 中间背包网格 (Scrollable Inventory)
- **宽度**: 280像素
- **高度**: 300像素
- **支持功能**:
  - 显示所有4个容器
  - 鼠标滚轮上下滚动
  - 拖拽移动物品
  - 跨容器移动支持

### 容器列表（从上到下）
1. **Pockets** (2×5) - 10格容量
2. **Tactical Vest** (8×8) - 64格容量
3. **Backpack** (12×20) - 240格容量
4. （可扩展更多容器）

## 控制方式

| 按键 | 功能 |
|------|------|
| **E** | 打开/关闭库存 |
| **I** | 打开库存（备选） |
| **鼠标滚轮** | 在库存中上下滚动 |
| **左键拖拽** | 移动物品到新位置 |
| **ESC** | 关闭库存 |

## 实现细节

### 技术架构
```
InventoryScreenOpenMixin
    ↓
    当E键打开原生InventoryScreen时被拦截
    ↓
    替换为 CustomInventoryScreen
    ↓
    显示 [Status Panel] + [Scrollable Grid] 布局
```

### 核心文件
- `src/client/java/.../ui/inventory/CustomInventoryScreen.java` - 主界面类
- `src/client/java/.../mixin/client/InventoryScreenOpenMixin.java` - 屏幕拦截
- `src/client/java/.../inventory/client/ClientInventoryCache.java` - 缓存管理

### 血量同步
- 血量数据实时从 `ClientHealthCache` 获取
- 每帧更新显示
- 支持动画过渡（使用lerp）

## 用户体验

### 打开库存流程
1. 玩家按 E 键
2. 原生InventoryScreen 被拦截
3. 替换为 CustomInventoryScreen
4. 显示左侧血量 + 中间背包

### 关闭库存流程
1. 玩家按 ESC 或点击外部
2. 屏幕关闭，返回游戏
3. 按键重新可用

### 滚动操作
- 鼠标指向中间背包区域
- 使用鼠标滚轮上下滚动
- 滚动偏移范围: 0-200像素

## 后续优化建议

1. **动画效果**
   - 打开/关闭过渡动画
   - 物品拖拽的虚影效果

2. **搜索功能**
   - 在中间添加搜索框
   - 快速查找物品

3. **排序功能**
   - 按类型、大小、最近使用排序

4. **容器过滤**
   - 只显示已装备的容器
   - 未装备容器显示为灰色

5. **拖拽改进**
   - 显示可放置的网格高亮
   - 拖拽时显示物品预览

6. **快捷键**
   - 快速切换容器
   - 快速堆叠物品

## 测试清单

```
基本功能：
☐ 按 E 键打开自定义库存
☐ 显示左侧血量状态面板
☐ 显示中间背包网格
☐ 按 ESC 关闭库存

滚动功能：
☐ 鼠标滚轮上下滚动背包
☐ 滚动偏移不超过200像素
☐ 滚动时不影响其他操作

血量显示：
☐ 显示所有6个身体部位
☐ 血量数值正确更新
☐ 血量条颜色正确渐变

物品操作：
☐ 鼠标拖拽移动物品
☐ 跨容器移动物品
☐ 碰撞检测生效
```

## 故障排除

### 问题1：按E键仍显示原生库存
**解决方案**: 
- 检查 InventoryScreenOpenMixin 是否在 mixins.json 中注册
- 检查Mixin注入点是否正确

### 问题2：背包不显示
**解决方案**:
- 检查 CustomInventoryScreen 是否正确初始化
- 验证 ClientInventoryCache.init() 是否被调用

### 问题3：血量不更新
**解决方案**:
- 检查 ClientHealthCache 是否收到健康数据包
- 验证 render() 方法每帧调用

### 问题4：滚动无效
**解决方案**:
- 检查鼠标位置是否在背包区域内
- 验证滚动事件处理逻辑

